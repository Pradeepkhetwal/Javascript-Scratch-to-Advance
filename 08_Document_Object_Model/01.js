//printing the document object.
//you can see in the console it contains all the details of the webpage.
// console.log(window.document); we can write this line as
console.log(document)//as window object is present by default so it will automatically link with window object.

//accessing an html element by it's id.Using document object method.
const username = console.log(document.getElementById("username")); //it will return an object of that element whose id is username. so we can store it in a const.


